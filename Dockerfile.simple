# 简单版 Dockerfile - 直接部署静态文件
FROM nginx:alpine

# 复制项目文件到 nginx 目录
COPY . /usr/share/nginx/html

# 复制 nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
